= simple_form_for(@variable_field_category) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :rgb
    = f.input :description
    = f.association :user

  .form-actions
    = f.button :submit, class: 'btn btn-primary'
